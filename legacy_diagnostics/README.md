# Legacy diagnostics

These files are retained only as historical evidence from the submitted ZIP.
They contain hard-coded Google Drive paths and some of their stored notebook
outputs refer to different checkpoints/code revisions. They are not part of
the corrected workflow. Use `audit_dataset.py`, `diagnose_checkpoint.py`,
`test.py`, and `eval.py` from the repository root instead.
